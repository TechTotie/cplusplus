int main() {}
// No need to return
// No need to include anything
// By default if the program executes correctly 0 is returned else non zero value, used in linux systems, windows ignores return value.
